import 'package:flutter/material.dart';

/// Solid fill behind `assets/images/app_logo.png` (sampled from image corners).
/// Use for scaffolds and bars so the logo sits flush with the UI chrome.
const Color kBrandCanvas = Color(0xFF12102A);
